name: Proof Readability V12 Temp

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/proof-readability-v12-temp.yml

permissions:
  contents: write
  actions: write

jobs:
  patch-test-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: worker/package-lock.json
      - name: Apply Proof readability V12
        run: python3 .github/dev-tools/proof-readability-v12-temp.py
      - name: Validate Proof V12 and quota guards
        run: |
          node --check worker/src/proof-ui-v10.js
          node --check worker/src/proof-plate-search-v5.js
          cd worker
          npm ci --no-audit --no-fund
          npm run check
          node --test tests/proof-command-center-v10.test.mjs tests/proof-smart-v8.test.mjs tests/auth-session-resilience.test.mjs
          grep -q 'PROOF_READABILITY_V12' src/proof-ui-v10.js
          grep -q 'PROOF_FD_LH_HEADER_V12' src/proof-ui-v10.js
          grep -q 'PROOF_DETAIL_ON_DEMAND_V12' src/proof-ui-v10.js
          grep -q 'PROOF_PLATE_HAR_V12' src/proof-plate-search-v5.js
          ! grep -q 'setInterval' src/proof-ui-v10.js
          echo 'PROOF_V12=PASS'
          echo 'NEW_BACKGROUND_API=0'
          echo 'NEW_POLLING=0'
          echo 'NEW_HISTORY_WRITES=0'
          echo 'TBR_CHANGED=NO'
          echo 'MS_MUTATION_TESTED=NO'
          echo 'PRODUCTION_TOUCHED=NO'
      - name: Commit functional Proof V12 changes
        run: |
          git config user.name 'Flashdevnak'
          git config user.email '26nak.testdev@gmail.com'
          git add proof.html worker/src/proof-ui-v10.js worker/src/proof-plate-search-v5.js worker/tests/proof-command-center-v10.test.mjs
          git commit -m 'Improve Proof readability and plate search'
          git pull --rebase --autostash origin main
          git push origin HEAD:main
      - name: Dispatch DEV deploy
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run deploy-worker-dev.yml --repo "$GITHUB_REPOSITORY" --ref main
